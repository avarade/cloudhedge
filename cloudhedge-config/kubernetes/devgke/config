kind: Deployment
apiVersion: extensions/v1beta1
metadata:
  name: frontend
  namespace: devgke-rhel-icescrum
spec:
  replicas: 1
  template:
    metadata:
      labels:
        deploy: frontend
        cbservice: frontend
    spec:
      containers:
        - name: frontend
          image: 'kingalech/sanity:dev'
          env:
            - name: CB_ENV_NAME
              value: devgke-rhel-icescrum
            - name: CB_CLUSTER_ID
              value: 5ca7480b8ba30400158ad285

---
kind: Service
apiVersion: v1
metadata:
  labels:
    service: frontend
    cbservice: frontend
  name: frontend
  namespace: devgke-rhel-icescrum
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      name: port80
  selector:
    deploy: frontend

